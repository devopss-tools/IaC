# Ansible included Variables
* control_path - ansible command line (root) path (./ansible/)
* playbook_dir - playbook dir
* role_path - path of current running role
* inventory_dir - ./env/  directory

