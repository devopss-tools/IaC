# for documentation of all options, see:
#   http://docs.mongodb.org/manual/reference/configuration-options/

# where to write logging data.
systemLog:
  destination: file
  logAppend: true
  path: /var/log/mongodb/mongod.log

# Where and how to store data.
storage:
  dbPath: /var/lib/mongodb
  journal:
    enabled: true
#  engine:
#  mmapv1:
#  wiredTiger:

# how the process runs
processManagement:
  fork: true  # fork and run in background
  pidFilePath: /var/run/mongodb/mongod.pid  # location of pidfile

# network interfaces
net:
  port: 27017
  bindIp: {{ ansible_currentHost_fact_ip }} # Listen to local interface only, comment to listen on all interfaces.

#security:

#operationProfiling:

replication:
   oplogSizeMB: 2
   replSetName: "{{ mongodb_deploy_config_replica_name }}"
   enableMajorityReadConcern: true

#sharding:

## Enterprise-Only Options

#auditLog:

#snmp:

#cfg = rs.conf()
#cfg.members[1].host = "mg4cp01.devopss.net:27017"
#rs.reconfig(cfg)